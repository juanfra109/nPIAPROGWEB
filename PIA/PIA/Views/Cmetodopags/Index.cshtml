@model IEnumerable<PIA.Models.dbModels.Cmetodopag>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table tablasadmin">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                <button asp-action="Edit" asp-route-id="@item.Idmetpag">Edit</button> |
                <button asp-action="Details" asp-route-id="@item.Idmetpag">Details</button> |
                <button asp-action="Delete" asp-route-id="@item.Idmetpag">Delete</button>
            </td>
        </tr>
}
    </tbody>
</table>
<button onclick="atras()">Volver a pagina Admins</button>

<script>
    var link = 'https://localhost:7095';
    $(document).ready;
    function atras(){
        location.href = link+'/Home/Admin';
    }
</script>